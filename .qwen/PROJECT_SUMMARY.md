The user wants me to generate a comprehensive project summary in markdown format based on the conversation history. Let me extract the key information:

1. **Overall Goal**: Fix and improve a TCN (Temporal Convolutional Network) model for multi-target grid forecasting (tao_load_mw, cable_flow_mw, kmb_flow_mw)

2. **Key Knowledge**:
- Project is at `/Users/chanthawat/Developments/gridtokenx-forecase-ml`
- Uses Python with uv for dependency management
- TCN model for sequential forecasting
- Three targets: tao_load_mw, cable_flow_mw, kmb_flow_mw
- Schema defined in `schema.py` (SEQ_FEATURES, TARGETS, TAB_FEATURES)
- Raw data from PEA AWS Sandbox (SCADA data, 15-min intervals)
- BESS SoC was broken (all zeros), STL decomposition was never called
- Scaler only covered 17 weather/grid features, not targets
- Target scaling is critical because kmb_flow_mw (σ=9.08) dominated MSE over tao_load_mw (σ=1.25)

3. **Recent Actions**:
- Fixed `data/preprocess.py`: called `impute_bess_soc` and `decompose_kmb` after column renaming
- Fixed BESS SoC detection to handle "65 then all zeros" edge case
- Updated `schema.py`: SEQ_FEATURES reduced from 28 to 22 (removed dead SoC and missing STL columns)
- Updated `models/tcn_model.py`: Added target scaler, feature scaler, early stopping, grad clipping, hard-fail on missing features
- Updated `config.yaml`: epochs 30→100, added early_stopping_patience: 15


- Regenerated processed parquets and verified convergence with a 5-epoch subset test showing train loss decreasing from 0.94 to 0.83 and val loss stabilizing around 1.0

4. **Current Plan**: The preprocessing pipeline now produces correct parquet files, and the TCN model is ready for training with proper scaling and regularization applied across all features.```markdown
# Project Summary

## Overall Goal
Fix and improve a multi-target TCN (Temporal Convolutional Network) model for grid forecasting on Thailand's island microgrid (Ko Tao, Phangan, Samui), addressing broken preprocessing, unscaled targets, and dead features that caused training divergence.

## Key Knowledge
- **Project root:** `/Users/chanthawat/Developments/gridtokenx-forecase-ml`
- **Python env:** `uv`-managed venv at `.venv/`, Python 3.11
- **Raw data source:** PEA AWS Sandbox — SCADA CSV/Parquet, 15-min intervals, 12-month history, per-substation
- **Centralized schema:** `schema.py` defines `SEQ_FEATURES` (22 features for TCN), `TAB_FEATURES` (for LightGBM), and `TARGETS` (`tao_load_mw`, `cable_flow_mw`, `kmb_flow_mw`)
- **Data pipeline:** `data/generate_dataset.py` → `data/preprocess.py` → train/val/test parquets → `models/tcn_model.py`
- **Scaler architecture:** The original `scaler.pkl` (StandardScaler) only covers 17 weather/grid features, NOT targets. Parquets are pre-scaled in-place for features only.
- **Critical scaling insight:** Targets have wildly different scales — `tao_load_mw` (σ=1.25), `cable_flow_mw` (σ=2.66), `kmb_flow_mw` (σ=9.08). Without target scaling, MSE is dominated by kmb by ~50× in squared error, causing train/val divergence.
- **TCN config:** window=192 (48h), horizon=96 (24h), filters=128, layers=4, batch=64, lr=0.001, dropout=0.2
- **Training runs on CPU** (no GPU available on this machine); full training is slow (~minutes per epoch on 75k rows)
- **MLflow tracking:** local SQLite, experiment `GridTokenX_TCN`
- **Build/run command:** `PYTHONUNBUFFERED=1 .venv/bin/python models/tcn_model.py` (or `just preprocess` / `just train`)

## Recent Actions
- **[DONE] Fixed dead code in preprocess.py:** `impute_bess_soc()` and `decompose_kmb()` are now called *after* column renaming (they reference `tao_load_mw`, `kmb_flow_mw` which are renamed from raw `Island_Load_MW`, `Samui_Circuit_MW`)
- **[DONE] Fixed BESS SoC detection:** Changed condition from `std() < 0.01` to also catch the "65.0 at row 0, then all zeros" edge case: `(nunique <= 2 and >95% zeros)`. SoC now has real dynamics (75k unique values, σ=1.0 after scaling).
- **[DONE] Updated schema.py:** `SEQ_FEATURES` reduced from 28 → 22 features by removing 3 dead SoC columns (`bess_soc_pct`, `phangan_soc_pct`, `samui_soc_pct` — all were zero) and 3 missing STL columns (`kmb_trend`, `kmb_seasonal`, `kmb_resid`). Note: STL columns are now generated by preprocess but excluded from TCN features to keep the model lean.
- **[DONE] Added target scaler to TCN training:** `WindowDataset` now accepts and applies a `target_scaler` (StandardScaler fitted on train targets). This balances MSE across all three targets.
- **[DONE] Added feature scaler to TCN training:** `WindowDataset` now accepts and applies a `feature_scaler` (StandardScaler fitted on all 22 SEQ_FEATURES). This eliminates mixed-scale issues (e.g., `hour_of_day` 0–23 mixed with scaled features at mean=0, std=1).
- **[DONE] Added early stopping + gradient clipping:** 15-epoch patience, grad clip at max_norm=1.0, increased max epochs from 30 → 100.
- **[DONE] Hard-fail on missing features:** `WindowDataset` now raises `ValueError` instead of silently zero-filling missing columns.
- **[DONE] Regenerated processed parquets:** Re-ran `preprocess.py` — confirmed BESS SoC has dynamics, STL columns exist, all 22 SEQ_FEATURES present, zero NaNs.
- **[DONE] Verified convergence:** 5-epoch test on 5k-row subset: Train loss 0.94→0.83 (decreasing), Val loss ~1.0 (stable in expected 0.1–1.2 range). Before fixes, val loss was exploding due to unscaled targets.

## Current Plan
1. [DONE] Fix preprocess.py dead code (BESS SoC + STL decomposition)
2. [DONE] Regenerate parquets with correct schema
3. [DONE] Add target scaler, feature scaler, early stopping to TCN training
4. [DONE] Verify convergence on data subset
5. [TODO] Run full TCN training on complete dataset (75k train rows) — will take significant time on CPU
6. [TODO] Investigate `phangan_soc_pct` and `samui_soc_pct` — still all zeros (separate island SoC columns not in TCN features, but present in TAB_FEATURES for LightGBM)
7. [TODO] Consider adding STL columns (`kmb_trend`, `kmb_seasonal`, `kmb_resid`) back into SEQ_FEATURES if model accuracy needs improvement — they are now correctly generated in the parquet
```

---

## Summary Metadata
**Update time**: 2026-05-10T11:13:35.546Z 
